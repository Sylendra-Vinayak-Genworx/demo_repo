version: "3.9"

services:
  backend:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - PORT=8000
    volumes:
      - .:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
